import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'Kai.4.Fit',
  appName: 'Kai-4-Fit',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
